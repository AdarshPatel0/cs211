#include <iostream>
#include <istream>
#include <string>

using namespace std;

int maiin(){
	int const digits = 100;
	char numA[digits] = {};
	char numB[digits] = {};
	cout << "First integer (up to 100 digits):";
	getline(cin, numA);
	return 0;
}