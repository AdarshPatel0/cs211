#include <iostream>

using namespace std;

int maiin(){
    int const digits = 100;
    int numA[digits] = {};
    int numB[digits] = {};
    cout << "First integer (up to 100 digits):";
    for (int i = digits-1; i!=-1;i--){
        if(cin.eof()) break;
        
    }
    return 0;
}